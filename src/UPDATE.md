# AIUI 更新日志

## V6.0.0 Release · 2026-08-11

## V6.0.0 Canary 3 · 2026-08-11

- 修复桌面协作横向字幕移动后右侧新文字仍被截断的问题，改为使用字幕容器的真实横向滚动并在流式文字更新时重新计算显示位置。
- AI 字幕仅在 TTS 开始朗读当前可视区域中无法完整显示的语句时滚动；前面已完整显示的语句不会提前触发移动。
- 用户语音识别字幕会随部分识别结果实时滚到最右侧，持续显示最新文字。

## V6.0.0 Canary 2 · 2026-08-11

- 图片缩略图打开大图与关闭大图增加淡入淡出动画。
- 桌面协作模式在 AI 播放及回声冷却期间抑制 VAD，避免应用自我聆听 AI 声音；普通语音聊天逻辑不变。
- 偏好设置“桌面协作”新增“跳过B组语音”，默认关闭；开启后仅播放 A 组和 C 组预制语音。
- 桌面协作顶部 AI 行与用户行使用横向自动滚动，完整文本不再被省略号替代。

## V6.0.0 Canary · 2026-08-10

- 新增桌面协作模式：透明无边框、始终置顶、底部停靠的横向字幕窗口，并在主窗口与结束按钮之间实现选择性点击穿透。
- 桌面协作启动后自动最小化主窗口，结束后恢复；协作文本、AI 语音记录和截图会写回对应主会话。
- 新增可配置的全局传图快捷键，默认 `Ctrl+A`；截图目标为鼠标所在显示器，多次触发只保留最后一张。
- 新增低、均衡、高三档截图精细度，默认压缩为最大边 1920px、JPEG 质量 85。
- 桌面协作 system prompt 固定以前置隐藏提示说明截图能力，并将语音回复的首句及单句长度限制放宽为原来的两倍。
- 语音聊天和桌面协作统一支持 `/responses` 与 `/chat/completions`，Responses 返回 501 时自动回退重试。
- 语音预制包新增 C 组“让我看一下啊……”“稍等，我看看……”“欸？我看到了……”，截图请求在 A 组后必定随机播放一条 C 组语音。
- 新增常驻托盘菜单，可打开主页面、新建或恢复最近会话的语音聊天/桌面协作，并在开发者模式下清除或导出日志。
- 主窗口关闭后改为隐藏到托盘，只有托盘“退出AIUI”会结束应用。
- 版本升级到 `V6.0.0 Canary`，Windows 产物更新为 Canary 命名。

## V5.7.0 · 2026-08-07

- 修复对/chat/completions接口的回复内容渲染逻辑问题。
- 修复对/responses接口回复内容分类判定不精确的问题。
- 用户选择/responses端口若遇到501错误将自动回退/chat/completions。

## V5.6.0 2 · 2026-08-04

- 加入了KaTeX渲染的字体，显示更加精确。
- 增强密钥安全性。

## V5.6.0 · 2026-08-04

- 修复手动停止 AI 生成后，灵动岛通知仍然常驻的问题；现在会像正常回复结束一样在短暂时间后自动消失。
- 继续完善 Responses API 的流式输出兼容，已能正确返回非流式回复，并在收到增量内容时实时刷新消息内容。
- 版本升级到 `V5.6.0`，并重新生成 Windows Portable / Setup 安装包。

## V5.5.0 · 2026-08-03

- 修复启用 Clash 等 Windows 系统代理时，主进程 Fish Audio 国内版 TTS 未遵循 Electron 代理规则而持续鉴权失败的问题。
- 实时语音聊天 TTS 改为在 `audiochat.html` 渲染进程直接请求国内版接口，Network 面板可直接看到每句语音请求，并保留请求、返回状态和音频字节日志。
- 修复主对话回放并行读取多段 MP3 时长后误判总时长、只播放第一句的问题；现在逐段读取并按片段索引连续播放完整回复。
- 修复回放切换片段时清空旧音频触发旧 `onerror`、导致后续语音被重复推进或跳过的问题。
- 修复清除语音记录后播放灵动岛异常闪现，以及清除流程重复定义导致的重复反馈。
- 语音回放改为按原始句子顺序连续播放该次 AI 回复中全部成功生成的语音片段。
- 回放灵动岛压缩为单行通知高度，移除回复文字和播放状态，只保留半宽进度条、暂停 / 继续图标与停止图标。
- 修复语音聊天第一条语音尚未成功获取或播放时，AI 字幕错误定位到最后一句的问题；现在默认将第一句话置于中间。
- 版本升级到 `V5.5.0`，并重新生成 Windows Portable / Setup 安装包。

## V5.4.0 · 2026-08-03

- 语音聊天模式生成的 AI 语音会按条保存到 IndexedDB。
- 主对话里，来自语音聊天模式的 AI 回复现在会显示“回放”按钮。
- 新增顶部“灵动岛”式语音回放条，支持暂停、停止和拖动进度；切换到另一条回放时会自动停止当前播放。
- 偏好设置“其他”新增“清除语音记录”，会通过带淡入淡出动画的警告弹窗删除所有非预制语音记录。
- 版本升级到 `V5.4.0`，并重新生成 Windows Portable / Setup 安装包。

## V5.3.0 · 2026-08-02

- 语音聊天字幕字号和行距略微收紧。
- AI 字幕支持按句自动跟随滚动，并允许用户手动滚动；5 秒无操作后恢复自动跟随。
- AI 字幕改为“每句完成后淡入”，人类字幕取消淡入。
- AI / 人类字幕区域隐藏系统滚动条但保留滚动能力。
- 压缩上下文确认弹窗按钮改为纵向独占一行布局。
- 偏好设置和压缩上下文确认弹窗新增淡入 / 淡出过渡。

## V5.2.0 · 2026-08-02

- 图像生成 / 编辑支持在提示词中写入 `16:9`、`4:3`、`1:1` 等宽高比。
- 自动把宽高比映射为图像请求尺寸。
- 图像重试会保留原始提示词并重新计算比例参数。

## V5.1.0 · 2026-08-02

- 重构语音聊天窗口布局，采用上下双分区黑白字幕界面。
- 语音聊天继续使用 Silero VAD、讯飞流式听写和 Fish Audio HTTP TTS。
- 语音聊天支持把完整的人类 / AI 轮次回传到当前文字对话。
- 新增模型列表编辑与上下文压缩能力。

## V5.0.0 · 2026-08-02

- 新增 Electron 语音聊天入口和独立 `audiochat.html` 页面。
- 新增 Fish Audio 预制语音生成与主进程文件存储。
- 新增讯飞流式语音听写和 Fish Audio HTTP TTS。
- 增加 Windows Portable / Setup 构建配置。
